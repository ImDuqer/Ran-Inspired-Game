using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;


/*public class CartaSpawn : MonoBehaviour
{
    public GameObject Samurai;
    public float MouseXSensibilidade = 1;
    public float MouseYSensibilidade = 1;

    void Start()
    {
        
    }

    void Update()
    {
        CriarSamurai();
    }

    public void CriarSamurai()
    {
        /*float moveX = Input.GetAxisRaw("Mouse X");
        float moveY = Input.GetAxisRaw("Mouse Y");

        Samurai.transform.position = new Vector3(moveX, moveY, transform.position.z);

        if (Input.GetKey(0))
        {
            Ray raio = Camera.main.ScreenPointToRay(Input.mousePosition);
            RaycastHit hit;

            if(Physics.Raycast(raio, out hit))
            {
                Instantiate(Samurai, hit.point, Quaternion.identity);
            }

        if(Input.GetButtonDown("Fire1"))
        {
            Vector3 mousePos = Input.mousePosition;
            mousePos.z = 2.0f;
            Vector3 objectPos = Camera.main.ScreenToWorldPoint(mousePos);
            Instantiate(Samurai, objectPos, Quaternion.identity);
        }

    }
}*/
